{
  "name": "drbsl-rest-api",
  "version": "1.0.0",
  "description": "rest api for drbsl",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd client && npm run build",
    "install-client": "cd client && npm install",
    "heroku-postbuild": "npm run install-client && npm run build",
    "start": "nodemon server.js",
    "client": "cd client && npm start"
  },
  "repository": {
    "type": "git",
    "url": "drbsl-node-api"
  },
  "author": "chauchausoup",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.0.0",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "jsonwebtoken": "^8.5.1",
    "leaflet": "^1.6.0",
    "mongoose": "^5.9.23",
    "react-leaflet": "^2.7.0"
  }
}
